services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    ports:
      - "3000:3000"
    volumes:
      - ascn_data:/app/data

volumes:
  ascn_data:
